.Side-bar
  .Side-bar__head-menu
    .top-items
      %h3.top-items--name 
        = current_user.name
      %ul.icons-list
        %li
          = link_to '#', class: "btn-edit" do
            =icon('fas', 'edit')
        %li
          = link_to edit_user_path(current_user), class: "btn-cog" do
            =icon('fas', 'cog')
  .Side-bar__group-list
    = link_to '#', class: "group-logo" do
      teccamp
    %p.group-message まだメッセージがありません